import paper from "../lib/Paper.json";
import paperManager from "../lib/PaperManager.json";

export const PAPER_MANAGER_CONTRACT = paperManager as Record<string | number, any>;
export const PAPER_CONTRACT = paper as Record<string | number, any>;
